import AuthShell from "@/components/AuthShell";
import React from "react";

const LoginPage = () => {
  return (
    <div>
      <AuthShell title="Login" sub="Masuk ke akun Anda" children={null} />
    </div>
  );
};

export default LoginPage;
